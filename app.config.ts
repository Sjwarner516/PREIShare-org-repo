/**
 * TanStack Start app config entry.
 * This starter runs on Vite (`vite.config.ts`); keep this file at the repo
 * root so PAUL and later course steps can find the same config surface.
 */
export { default } from './vite.config'
